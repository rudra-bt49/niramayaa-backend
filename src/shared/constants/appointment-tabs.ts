export enum patient_appointment_tabs {
    SCHEDULED = 'scheduled',
    PENDING_PAYMENT = 'pending_payment',
    HISTORY = 'history'
}

export enum doctor_appointment_tabs {
    ONGOING = 'ongoing',
    SCHEDULED = 'scheduled',
    HISTORY = 'history'
}
